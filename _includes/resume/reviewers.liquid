<div class="card-text font-weight-light">

  {% assign reviewers = data[1] %}

  {% for section in reviewers %}
    <div class="mb-4">

      <!-- Section title -->
      <h5 class="reviewer-section-title mb-2">
        <i class="{{ section.icon }}"></i>
        {{ section.name }}
      </h5>

      <!-- Keywords list -->
      <ul class="list-unstyled ml-3">
        {% for item in section.keywords %}
          <li class="mb-1">
            {{ item }}
          </li>
        {% endfor %}
      </ul>

    </div>
  {% endfor %}

</div>
